<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>DNSimple / Heroku Connector</title>
    <link rel="stylesheet" href="https://make-it-nice.dnsimple.com/css/dnsimple-tachyons-1.0.0.min.css">
  </head>

  <body class="dn-body">
    <div class="dn-navbar pa3 mb4">
      <div class="dn-navbar-branding">
        Heroku Connector
      </div>

    <%= if HerokuConnector.Plug.CurrentAccount.account_connected?(@conn) do %>
      <div class="dn-navbar-nav">
        <%= link "Connections", to: connection_path(@conn, :index), class: "dn-link-primary mh2" %>
        <%= link "Logout", to: account_path(@conn, :delete), method: :delete, class: "dn-link-primary mh2", form: [class: "dib"] %>
      </div>
    <% end %>
    </div>

    <div class="container">

      <div class="mw7 center">
        <%= if get_flash(@conn, :info) do %>
          <div class="dn-alert-info" role="alert">
            <span><%= get_flash(@conn, :info) %></span>
          </div>
        <% end %>
        <%= if get_flash(@conn, :error) do %>
          <div class="dn-alert-danger" role="alert">
            <span><%= get_flash(@conn, :error) %></span>
          </div>
        <% end %>
      </div>

      <div class="mw7 mb4 center">
        <%= render @view_module, @view_template, assigns %>
      </div>
    </div>

    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
